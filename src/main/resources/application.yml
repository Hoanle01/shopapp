server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shopapp
    username: root
    password: 29102001h
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

api:
  prefix: api/

jwt:
  expiration: 2592000
  secretKey:"I'm Hoang"
